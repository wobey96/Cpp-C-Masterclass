#include <iostream>
#include <string> 
#include <cmath>
#include <algorithm>
#include <vector>
#include <array>
#include <unordered_map>
#include <utility>

std::vector<std::vector<int>> returnMax()
{
    std::vector<std::vector<int>> myMatrix;
    return myMatrix; 
}

int main()
{
    std::vector<std::vector<int>> myVector = returnMax(); 
    // std::vector<std::vector<int>> myMatrix; 
    if(3 == myVector.size())
    {
        std::cout << "Hello World" << std::endl; 
    }
    return 0; 
}