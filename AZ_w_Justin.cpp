#include <iostream>
#include <queue>
#include <functional>
#include <string> 
#include <cmath>
#include <algorithm>
#include <vector>
#include <array>
#include <unordered_map>

// distance calculation function 
double calcDist(int X_2, int Y_2)
{

    //  sqrt( (X_2 - X_1)^2  + (Y_2 - Y_1)^2 )

    int X_1 = 0; 
    int Y_1 = 0; 
    double X_calc = pow(X_2 - X_1, 2); 
    double Y_calc = pow(Y_2 - Y_1, 2);
    double finalCalc = sqrt( X_calc + Y_calc);  


}
// 

std::vector<std::vector<int>> returnDistance(std::vector<std::vector<int>> allLocations, int numRestaurants)
{

}


int main()
{
    // starting location: 0,0
    std::vector<std::vector<int>> locations { {1,2}, {3,1}, {2,2}, {9,0} };
    int restAmount = 2;  


        map<int, double> 
        int k = 0; 
        map.insert(std::make_pair([i][j], distacneFormula(locations[i], locations[i][j]) ))
    

    std::priority_queue<std::pair<std::vector<int>, double>>
    // calc distance
    {1,2}, 1.21 - pair --> into minHeap 
    {0}, {1,2} -->into unordered_map<int, std::vector<int>> 
    [0] = 1.21, {1,2}
    [1] = 2.8, {2,2}
    [2] = 3.1 {3,1}
    std::vector<std::vector<int>> retVec{ {1,2}, {2,2} } [restAmount][2]
    pair<std::vector<int>> tempPair = priority_queue.top();
    tempPair.first
    add retVec.push_back(tempPair.first); 



}